import React from 'react';
import './styles.scss';
import Navbar from '../Navbar/Navbar'

const Header = props => {
  return (
    <header className = "header">
      <Navbar/>
    </header>
  );
};

export default Header;
 